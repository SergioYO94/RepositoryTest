name: Deploy on PR

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - dev

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Clona l'intera cronologia e tutti i branch
      - name: Set up environment
        run: echo "Deploying because PR from B to A was created or updated"

      - name: Run deployment script
       
        run: |
          echo "Starting deployment..."
          git pull origin
          git checkout f2

          git reset --hard HEAD~1
          git push --force
